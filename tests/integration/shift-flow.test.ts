import { describe, expect, it } from 'vitest';

describe.skip('Flujo completo de turno (integration)', () => {
  it('placeholder - requiere Supabase test env', () => {
    expect(true).toBe(true);
  });
});
